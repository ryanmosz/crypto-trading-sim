// Environment configuration for crypto-trading-sim
// Copy this to env.js and fill in your values

const ENV = {
  // Your Supabase project URL
  SUPABASE_URL: 'YOUR_SUPABASE_URL_HERE',
  
  // Your Supabase anonymous (public) key - safe for frontend
  SUPABASE_ANON_KEY: 'YOUR_SUPABASE_ANON_KEY_HERE'
  
  // Note: Never put service role key in frontend code!
};

// Export for use in other modules
if (typeof module !== 'undefined' && module.exports) {
  module.exports = ENV;
} 

// Also make available in browser
if (typeof window !== 'undefined') {
  window.ENV = ENV;
} 